# 1，YAML的格式
    人类可读的配置格式，缩进=层级
    支持注释，声明规则不写代码
    冒号后空格、禁 Tab、键重复静默覆盖

# 2，pyyaml 翻译官
    把 YAML 翻译成 Python 字典的库
    必须 safe_load，防任意代码执行

# 3，统一返回 schema
    所有 check 函数返回同骨架 dict，骨架就是每个check函数返回的字典
    规则引擎/告警/看板能统一处理
    
# 4，给项目装上cli命令行入口
    checker 逻辑全有了，但每次检测都得打开 Python 手动 import、手动拼 DataFrame。文档计划里第一阶段的目标是「最小可运行版本」，标志就是**能跑起来**；而且将来接 Airflow 定时调度、面试现场 demo，靠的都是这个入口
# 5,使用`argparse`—— 命令行参数解析（`--table`、`--data` 这种）


# 6，复用 `load_rules` + `run_checks`，把规则引擎串成完整链路


# 7，结果格式化输出（终端里看清哪些规则过了、哪些挂了）


# 目前的问题
 1. rules.yaml 里的 checks（表级规则）被静默忽略了
  config/rules.yaml:11-13 声明了 row_count，但 core/checker.py:95 的 run_checks 只遍历 columns，根本没人执行
  checks。更危险的是 checker.py:113：未知规则名直接 continue，配置写了、代码没有 → 不报错、不警告，静默跳过——这正是你
  09-09 笔记里说"契约测试要防的事"，但你的契约测试（tests/test_config_loader.py:32）只遍历了 columns，没覆盖
  checks，所以没拦住。
  → 建议：实现 check_row_count + 表级调度，再把契约测试扩到 checks 段。另外 checks 里写的是 min: 1，和 range 的
  min_value 命名不统一，按你自己定的规矩"规则文件跟着代码走"对齐。

  2. CLI 入口（你 09-10 笔记已列入计划，接着做）
  现在每次检查都要开 Python 手动拼 DataFrame。建议：
  - core/cli.py：argparse，--table / --data 两个参数
  - pyproject.toml 加 [project.scripts] dqm = "core.cli:main"，pip install -e . 后一条命令就能跑
  - data/ 现在是空的（README 说放 .gitkeep，实际没放），放一个 sample_orders.csv 配合 CLI 演示——这直接服务你"面试现场
    demo"的目标

  3. CI 一次都没配
  你笔记里写了"CI 每次跑测试时自动检查"，但仓库里没有 .github/workflows/。加一个最简单的 ci.yml（pytest + ruff
  两步），你学的 git/GitHub 流程就闭环了。