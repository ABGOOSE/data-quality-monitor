# 1，CI 里写了 `ruff format --check .`（**整个仓库**）
    ruff 会**连 markdown 里的 Python 代码块一起检查**—— 你的学习日志 `notes/daily/2026-09-10.md` 里第 41 行有个代码块格式不过关：
   解决办法：
    在 pyproject.toml 给 ruff 加 `exclude`，把 `notes/` 排除出去。